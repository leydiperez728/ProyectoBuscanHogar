<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.buscandohogar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReportesSecondActivity"></activity>
        <activity android:name=".CachorrosActivity" />
        <activity android:name=".DireccionSecondActivity" />
        <activity android:name=".CampanaSecondActivity" />
        <activity android:name=".PreguntasActivity" />
        <activity android:name=".VeterinariaActivity" />
        <activity android:name=".ReportesActivity" />
        <activity android:name=".DireccionActivity" />
        <activity android:name=".CampanaActivity" />
        <activity android:name=".EsterillizarActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".ContactosActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>